---
const { slices } = Astro.props;
import ContentSlice from "./slices/ContentSlice.astro";
---

<div>
  {
    slices.map((slice: any) => {
      switch (slice.slice_type) {
        case "code":
          return null;
        case "content":
          return <ContentSlice slice={slice} />;
        default:
          throw new Error(`Unknown slice type: ${slice.slice_type}`);
      }
    })
  }
</div>
